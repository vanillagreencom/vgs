# backend/vendor/

Byte-exact copies of upstream Go modules. Read them and report real defects, but never propose an in-repository patch, refactor or style fix: the tree must match upstream exactly or vendor verification fails. Frame a genuine finding as an issue to file against the module, and say so explicitly.
