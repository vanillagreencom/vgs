# Clock

Date and time in the bar.

## Features

- Shows the date and time in the format you choose.
- Ticks once a minute, or once a second when the format shows seconds.

## Settings

- `format` on the widget's layout entry in `~/.config/vgs/shell.json`, in Qt date format. Default: `ddd d MMM  HH:mm`.
