# shell/plugins/

First-party plugins, one directory per plugin id, each with `manifest.json` at its root. The contract is `docs/architecture/plugins.md`. The vgs-plugin skill holds the rules, the templates and the API reference; load it before changing anything here.
