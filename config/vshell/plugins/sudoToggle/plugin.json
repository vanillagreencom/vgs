{
    "id": "sudoToggle",
    "name": "Passwordless Sudo",
    "description": "Toggle persistent passwordless sudo for your user. Runs `vshell sudo-toggle`, which installs or removes a validated /etc/sudoers.d drop-in; needs sudo and visudo (see `vshell deps check sudo-toggle`).",
    "version": "1.0.0",
    "license": "MIT",
    "author": "VanillaGreen",
    "icon": "gpp_maybe",
    "component": "./SudoToggleWidget.qml",
    "requires_shell": ">=1.0.0",
    "permissions": []
}
