# ASDControl

`asdcontrol.cpp` is vendored from <https://github.com/nikosdion/asdcontrol> at version 0.4.

ASDControl controls Apple Studio Display / Pro Display XDR brightness through Linux `hiddev` monitor-control reports. It is GPL-2.0-or-later; keep source available when shipping `bin/vshell-asdcontrol`.
