# Terminal-only ANSI slots. colors.toml holds the upstream terminal values
# under D017; this slot is a VGS green the upstream publishes nowhere. The
# shell's derived roles, pi and every other app template keep the palette's
# slot.

# bright green: added words in a diff.
color10 = "#3eb419"
