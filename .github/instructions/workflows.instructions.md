---
applyTo: ".github/workflows/**"
excludeAgent: "cloud-agent"
---

<!-- generated by bot-instructions 2.1.0 from kendex.toml, .kendex-generated.json, SKILL.md, schemas/renders.md, AGENTS.md. Edit [bot-instructions] in the effective manifest or the spec copy, then re-render. -->

Keep CI coverage consistent with scripts/validate; a step that runs a script the tree does not hold is a defect. The qml area needs a Wayland session and is not run on the runner. Whitespace checks must use the event's full change range and fail if the base cannot be resolved. The ci-ok context name stays stable.
